---
import Layout from '../layouts/Layout.astro';
const BASE = import.meta.env.BASE_URL;
---

<Layout title="關於" description="關於拾頁書室，以及這個閱讀空間的設計方式。">
  <section class="grid gap-8 lg:grid-cols-[1.1fr_0.9fr] lg:items-center">
    <div>
      <p class="eyebrow">About this place</p>
      <h1 class="page-title mt-3">讀過的書，<br />也是生活的索引。</h1>
      <p class="mt-6 max-w-xl text-base leading-8 text-stone-600">
        拾頁書室是一個個人閱讀記錄空間。它不追求把閱讀變成競賽，而是替每一次相遇留下線索：當時讀了什麼、想了什麼，又被哪一句話停住。
      </p>
      <div class="mt-8 flex flex-wrap gap-3">
        <a href={`${BASE}books/`} class="rounded-full bg-stone-900 px-5 py-2.5 text-sm font-bold text-white transition hover:bg-clay">逛逛書庫</a>
        <a href={`${BASE}rss.xml`} class="rounded-full border border-stone-300 bg-white/60 px-5 py-2.5 text-sm font-medium text-stone-700 transition hover:border-orange-300 hover:text-clay">訂閱 RSS</a>
      </div>
    </div>

    <div class="relative rounded-[2rem] bg-[#d9e0d4] p-7 sm:p-10">
      <span class="absolute right-8 top-6 font-serif text-7xl leading-none text-white/60" aria-hidden="true">“</span>
      <blockquote class="relative pt-10 font-serif text-2xl font-semibold leading-relaxed text-stone-800">
        閱讀，是借別人的眼睛，再看一次自己的世界。
      </blockquote>
      <div class="mt-8 h-px w-16 bg-sage/40"></div>
      <p class="mt-4 text-xs font-bold uppercase tracking-[0.2em] text-sage">Notes from the shelf</p>
    </div>
  </section>

  <section class="mt-16 grid gap-4 md:grid-cols-3">
    {[
      { number: '01', title: '保存相遇', text: '收進想讀、正在讀與已讀完的書，讓每段閱讀都有它的位置。' },
      { number: '02', title: '寫下痕跡', text: '用 Markdown 留下心得與書摘，格式開放，也方便長久保存。' },
      { number: '03', title: '回望旅程', text: '透過閱讀足跡與主題統計，重新看見自己長期關心的事。' }
    ].map((item) => (
      <article class="surface rounded-[1.4rem] p-6">
        <span class="font-serif text-sm font-bold text-clay">{item.number}</span>
        <h2 class="mt-5 font-serif text-xl font-semibold text-stone-900">{item.title}</h2>
        <p class="mt-3 text-sm leading-7 text-stone-500">{item.text}</p>
      </article>
    ))}
  </section>

  <section class="mt-12 rounded-[1.6rem] bg-stone-900 px-6 py-8 text-stone-300 sm:px-9 sm:py-10">
    <div class="grid gap-6 md:grid-cols-[1fr_auto] md:items-center">
      <div>
        <p class="text-[0.68rem] font-bold uppercase tracking-[0.2em] text-orange-300">Made to last</p>
        <h2 class="mt-3 font-serif text-2xl font-semibold text-white">資料始終在自己手上</h2>
        <p class="mt-3 max-w-2xl text-sm leading-7 text-stone-400">網站由 Astro 建置，每本書都是一份獨立的 Markdown 檔案。沒有封閉格式，也不必依賴遠端資料庫。</p>
      </div>
      <a href={`${BASE}export/`} class="shrink-0 rounded-full bg-white px-5 py-2.5 text-sm font-bold text-stone-900 transition hover:bg-orange-100">前往資料匯出</a>
    </div>
  </section>
</Layout>
